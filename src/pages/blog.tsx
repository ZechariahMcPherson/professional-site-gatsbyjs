import React, { FunctionComponent } from "react";
import { Layout } from "../components/Layout";

const Blog = () => {
  return (
    <Layout>
      I am blog page
    </Layout>);
};

export default Blog;
